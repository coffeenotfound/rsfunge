use crate::interpreter::fingerprint::Fingerprint;

pub struct AlphabetInstructionTable {
	
}

impl AlphabetInstructionTable {
	
	pub fn push_fingerprint(&mut self, fingerprint: &Fingerprint) {
		
	}
}

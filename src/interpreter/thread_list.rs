use crate::interpreter::FungeThread;

/// A list of funge threads with the appropriate manipulation methods.
pub struct ThreadList<'s> {
	threads: Vec<FungeThread<'s>>,
}
impl<'s> ThreadList<'s> {
	
}
